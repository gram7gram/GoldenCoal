const Api = require('../../src/api')
const tokens = require('../config/parameters').tokens

const controller = (server) => {

    // server.post(Api.POST.contacts, (req, res) => {
    //
    // })
}

module.exports = controller